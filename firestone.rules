rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all users for all documents
    // IMPORTANT: This is for development only! Restrict in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}